
//
//
// import {req, withParams} from 'vuelidate/lib/validators/common'
//
// // export default (obj) =>
// //   withParams({type: 'between', min, max}, value =>
// //     !req(value) || (!/\s/.test(value) && +min <= +value && +max >= +value))
//
//
// // export default (obj) =>
// //   withParams({type: 'between', min, max}, value =>
// //     !req(value) || (!/\s/.test(value) && +min <= +value && +max >= +value))
//
// var _common = require('vuelidate/lib/validators/common');
//
// var emailRegex = /^1[34578]\d{9}$/;
//
//
// export default  (0, _common.regex)('phone', emailRegex);
